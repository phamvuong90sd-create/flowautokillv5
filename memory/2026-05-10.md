
## 2026-05-10 — Flow Auto / Audio Prompt Studio durable notes

- Continued work on Flow Auto active repo: `https://github.com/phamvuong90sd-create/flowautokillv5`.
- Applied FlowKit-style selector logic to `apps/flow_auto_electron/payload/scripts/flow_batch_runner.py` and mirrored to `scripts/flow_batch_runner.py`.
  - Supports switching image/video mode via icon group (`image`, `videocam`).
  - Supports video sub-mode switching (`chrome_extension` for ingredients, `crop_free` for frames) with Vietnamese/English text fallbacks.
  - Supports aspect/size selection: `16:9`, `9:16`, `square`, `landscape_4_3`, `portrait_3_4` mapped to Flow crop icons.
  - Supports output count `x1` to `x4` / `1x` to `4x`.
  - Supports model switching for video and image models with aliases:
    - `veo3_fast` → Veo 3.1 Fast variants
    - `veo3_lite` → Veo 3.1 Lite variants
    - `veo3_quality` → Veo 3.1 Quality variants
    - `nano_banana_pro`, `nano_banana2`, `nano_banana`, `imagen4`
  - Added verification result objects: `typeRes`, `subRes`, `ratioRes`, `countRes`, `modelRes`.
- Flow Auto commit pushed: `11a36a7 fix(flow): unify FlowKit-style setting selectors`.
- Flow Auto GitHub Actions builds triggered for commit `11a36a7`:
  - Modern UI run `25593407080`
  - Protected Setup run `25593407635`
  - Earlier same-commit runs also existed: `25593368710`, `25593368707`, `25593368657`.

- Continued work on Audio Prompt Studio repo: `https://github.com/phamvuong90sd-create/audio-prompt-studio`.
- User requested UI updates to add prompt copy and TXT download.
- Added Electron IPC `dialog:saveText`, preload `saveText`, UI actions `copyPrompts()` and `downloadTxt()`.
  - Result area now displays plain prompt text instead of raw JSON where possible.
  - Added buttons: `Copy prompt` and `Tải TXT`.
- Audio Prompt Studio commit pushed: `2a7baa6 feat: add copy and txt export for prompts`.
- Audio Prompt Studio builds for `2a7baa6` completed successfully:
  - runs `25630580721`, `25630580687`.

- User then sent a UI reference image and requested Audio Prompt Studio interface to match it.
- Redesigned Audio Prompt Studio UI into modern dark layout:
  - Left sidebar contains Gemini API keys, transcription mode, dialog/subtitle toggles, style JSON, extra requirements, primary start/save buttons.
  - Main area contains Audio input card, Original Text card, and Result Prompt card.
  - Result card includes compact icon buttons for copy and download.
  - CSS rewritten with dark blue theme, card layout, badges, modern controls.
- Audio Prompt Studio commit pushed: `221bba4 feat: redesign UI matching user image template`.
- Audio Prompt Studio GitHub Actions builds triggered for `221bba4`:
  - run `25632144702` in progress
  - run `25632144579` queued

- Important user preferences reinforced:
  - Always commit, push, and trigger GitHub Actions after code changes.
  - Active Flow Auto repo is `flowautokillv5`.
  - Audio Prompt Studio should stay English-only for generated video prompts and avoid braces/slashes in prompts.
